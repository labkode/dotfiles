if [ -z "$SSH_AUTH_SOCK"   ] ; then
    eval `ssh-agent -s`
        ssh-add
fi

export GOPATH=$HOME/Developer/Go
export PATH=$PATH:$GOPATH/bin

alias red="cd /Volumes/red"
alias repo="red; cd repo"
alias clawiodev="cd $GOPATH/src/github.com/clawio"
